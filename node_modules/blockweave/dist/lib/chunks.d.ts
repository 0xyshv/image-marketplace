import { TransactionChunkResponseInterface, TransactionOffsetResponseInterface } from '../faces/lib/chunks';
import Api from './api';
export default class Chunks {
    private api;
    constructor(api: Api);
    getTransactionOffset(id: string): Promise<TransactionOffsetResponseInterface>;
    getChunk(offset: string | number | BigInt): Promise<TransactionChunkResponseInterface>;
    getChunkData(offset: string | number | BigInt): Promise<Uint8Array>;
    firstChunkOffset(offsetResponse: TransactionOffsetResponseInterface): number;
    downloadchunkedData(id: string): Promise<Uint8Array>;
}
